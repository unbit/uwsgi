CC=gcc

PYTHON_CFLAGS=`python2.4-config --cflags`
PYTHON_LIBS=`python2.4-config --libs`

CFLAGS=$(PYTHON_CFLAGS) -DUNBIT _GNU_SOURCE
LD_FLAGS=$(PYTHON_LIBS) -DUNBIT _GNU_SOURCE

PROGRAM=/usr/share/unbit/uwsgi24

all:	clean uwsgi

uwsgi:	main.o
	$(CC) $(LD_FLAGS) main.o -o $(PROGRAM)

main.o:	uwsgi.c
	$(CC) -c $(CFLAGS) -o main.o uwsgi.c
	
clean:
	rm -f main.o
